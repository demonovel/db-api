import { EnumApiType } from './types';
export declare function newFileURL(rootKey: string, dataKey: string | number, server?: string, type?: EnumApiType): URL;
export declare function newLinkURL(rootKey: string, dataKey: string | number, server?: string): URL;
